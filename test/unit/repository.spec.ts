import { Aurelia, Container, FrameworkConfiguration } from 'aurelia-framework';

import { configure, Repository } from '../../src/aurelia-collection';
import { TestSchema } from './helpers';

describe('repository', () => {
  it('should create a collection with only key and SchemaClass is provided', done => {});

  it('should create a collection with key, SchemaClass and baseUrl is provided', done => {});

  it('should create a collection with key, SchemaClass, baseUrl and CollectionClass is provided', done => {});

  it('should create a collection with key, SchemaClass, baseUrl and CollectionClass is provided', done => {});
});
