import { Aurelia, Container, FrameworkConfiguration } from 'aurelia-framework';

import { configure, Repository } from '../../src/aurelia-collection';
import { TestSchema } from './helpers';

describe('repository', () => {
  it('should create a collection with only key and SchemaClass', done => {});

  it('should create a collection with key, SchemaClass and baseUrl', done => {});

  it('should create a collection with key, SchemaClass, baseUrl and CollectionClass', done => {});

  it('should create a collection with key, SchemaClass, baseUrl CollectionClass and modelid', done => {});
});
